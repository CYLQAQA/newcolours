{
  "name": "sales-dashboard",
  "version": "3.0.0",
  "private": true,
  "description": "Sales dashboard (Monthly Report + Last Selling Price views). Loads app.min.js at runtime.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "terser": "^5.30.0"
  }
}
